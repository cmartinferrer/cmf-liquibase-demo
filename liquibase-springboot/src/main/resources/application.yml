spring:
    application:
        environment: dev

    liquibase:
        change-log: classpath:/liquibase/${spring.application.environment}/db.changelog-master.xml

    datasource:
        url: jdbc:mysql://localhost:3306/db
        username: user
        password: password
        driver-class-name: com.mysql.cj.jdbc.Driver
